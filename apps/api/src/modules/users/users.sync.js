/**
 * Kept for backwards compatibility. Staff users are now read and written
 * directly in PostgreSQL by users.repository.js, so there is nothing to mirror.
 */
export async function syncStaffUser() {}
